// Simple active session fixture indicating which business profile is currently active
export const activeSession = {
  activeUserId: "u-alex",
  activeBusinessProfileId: "bp-alex-northstar",
};

export default activeSession;
